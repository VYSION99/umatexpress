# Dependencies and build output
node_modules/
dist/
.next/
/out/
/coverage

# Local runtime state
.sites-runtime/
.wrangler/
*.tsbuildinfo

# Secrets and local environment
.env
.env.local
.env.*.local
!.env.example
*.pem

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editors and operating systems
.DS_Store
.vscode/
.idea/

# Agent scratch worktrees (full checkouts of this repo)
.kilo/

# Platform
.vercel
/.pnp
.pnp.js
next-env.d.ts

# NOTE: the root `build/` directory is source (the platform Vite plugin), not
# build output, so it is deliberately not ignored here.
